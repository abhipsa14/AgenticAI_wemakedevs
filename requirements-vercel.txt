# Vercel-optimized requirements (lightweight for serverless)
# Removed: chromadb, sentence-transformers (too memory-heavy)
# Using OpenAI embeddings API instead of local models

# Core Framework - pinned for binary wheels
fastapi==0.109.0
python-multipart==0.0.6
python-dotenv==1.0.0
jinja2==3.1.2
aiofiles==23.2.1

# Database - minimal sqlmodel
sqlmodel==0.0.14

# Pydantic for data validation
pydantic==2.5.3
pydantic-settings==2.1.0

# PDF Processing - lightweight
pypdf==3.17.4

# LLM Providers (API-based, lightweight)
openai==1.6.1
groq==0.4.2

# HTTP client
httpx==0.26.0
